backend:
  name: git-gateway
  branch: master

media_folder: static/assets
public_folder: /img

collections:
  - name: 'song'
    label: 'Song'
    folder: 'songs'
    create: true
    slug: '{{slug}}'
    fields:
      - { name: 'title', label: 'Title' }
      - { name: 'artist', label: 'Artist' }
      - { name: 'key', label: 'Key' }
      - { name: "chordpro", label: "ChordPro?", widget: "boolean", default: true }
      - { name: 'youtube', label: 'YouTube' }
      - { name: 'body', label: 'Body', widget: 'markdown' }